
const initialState = {
    toys: [],
    isLoading: false,
    // filterBy: toyService.getDefaultFilter(),
}

export function toyReducer(state = initialState, action = {}) {
    switch (action.type) {
        default:
            return state
    }
}